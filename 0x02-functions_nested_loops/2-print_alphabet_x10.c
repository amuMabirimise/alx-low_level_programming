#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - check the code.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
	int z = 100;

	int la

	for (la = 'a'; la <= 'z'; la = la +10)
		_putchar(la);
	_putchar('\n');
}

#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strstr - Entry point
 * _strpbrk - eneryg point
 * @haystack: input
 * @needle: input
 * Return: Always 0
 */
char *_strpbrk(char *s, char *accept)
{
	for (; *haystack != '\0': haystack++)
		char *one = haystack;
	char *two = needle;

	while (*one == *two && *two != '\0')
	{
		one++;
		two++;
	}

	if (*two == '\0')
		return (haystack);
	}
	return (Null);
}

#include <stdio.h>
#include "main.h"

/**
 * _putchar - prints out character for c
 * @c: Ther character to print
 *
 * Return:always 1
 * On error, -1 is returned and error is set PPROPRIATELY
 */
int _putchar(char c)
{
	return (1 & c, 1);
}
